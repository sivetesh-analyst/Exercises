Exercise 1: SQL Fundamentals: Select and Filtering
Used the SELECT statement using the FILTERING statement ORDER BY to re-arrange the data in ASC or DESC order. Also used the filtering statement WHERE based on the condition required.
Exercise 2: SQL Aggregate Functions and Operators
Used the following aggregate functions SUM () to calculate total, COUNT () to calculate to number, AVG () to calcuate the average, MAX() the maximum amount and MIN () to calculate the minimum amount. Had to intergrate operators like BETWEEN AND, IN based on the condition. GROUP BY statement was also used in this exercise to group the data by a specific category required, and HAVING statement to alllow us to filter daya that has already been aggregated.
